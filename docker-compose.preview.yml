version: "3"

services:
  platform:
    image: registry-vpc.cn-beijing.aliyuncs.com/letec/authmore-platform
    command: ["--spring.profiles.active=test"]
    container_name: platform
    restart: unless-stopped
    networks:
      - authmore
    ports:
      - "8086:8086"
    depends_on:
      - mongo
      - redis

  admin:
    image: registry-vpc.cn-beijing.aliyuncs.com/letec/authmore-admin
    command: ["--spring.profiles.active=test"]
    container_name: admin
    restart: unless-stopped
    networks:
      - authmore
    ports:
      - "8083:8083"
    depends_on:
      - mongo
      - redis

  authmore-ui:
    image: registry-vpc.cn-beijing.aliyuncs.com/letec/authmore-ui
    container_name: authmore-ui
    restart: unless-stopped
    networks:
      - authmore
    ports:
      - "3002:80"
    depends_on:
      - admin

  redis:
    image: redis
    restart: unless-stopped
    container_name: authmore-redis
    networks:
      - authmore
    volumes:
      - redis-data:/data

  mongo:
    image: mongo
    restart: unless-stopped
    container_name: authmore-mongo
    networks:
      - authmore
    volumes:
      - mongo-data:/data/db
      - mongo-config:/data/configdb

networks:
  authmore:

volumes:
  mongo-data:
  mongo-config:
  redis-data: