pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}
rootProject.name = 'authmore'

include ":auth", ":common"
project(":auth").projectDir = file("../auth")
project(":common").projectDir = file("../common")
